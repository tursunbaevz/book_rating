<div class="container">
    <div class="row">
      	
   	   <% @books.each do |book| %>
         <div class="col-md-4">
      		<a href="/books/<%= book.id %>"><%= image_tag book.image.url(:medium), class: 'book' %></a>
         </div>
    	<% end %>

    </div>
</div>
